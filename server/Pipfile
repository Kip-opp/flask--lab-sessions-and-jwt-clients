[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=2.2.2"
flask-sqlalchemy = ">=3.0.3"
flask-migrate = ">=4.0.0"
flask-bcrypt = ">=1.0.1"
flask-cors = ">=4.0.0"
pyjwt = ">=2.8.0"
python-dotenv = ">=1.0.0"
faker = ">=15.3.2"

[dev-packages]
pytest = ">=7.2.0"
pytest-cov = ">=4.0.0"

[requires]
python_version = "3.11"
